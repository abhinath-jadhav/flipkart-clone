import React, { useEffect, useState } from "react";

const SearchBody = ({ className }: { className: string }) => {
  return <div className={`${className} bg-white`}></div>;
};

export default SearchBody;
